# ПРАКТИКА: СРАВНЕНИЕ ТИПОВ КЕШИРОВАНИЯ

## ЦЕЛЬ

Сделать три простых примера одной и той же системы с разными типами кеширования и сравнить их в одинаковых условиях:

- `Lazy Loading` / `Cache-Aside` / `Write-Around`;
- `Write-Through`;
- `Write-Back`.

## ФОРМАТ

Одна и та же система должна быть реализована в трех вариантах.

Минимальный состав:

- `load-generator`; (например Jmeter или самописный)
- `application`;
- `cache`; (Redis/Apache Ignit/etc...)
- `БД`.

Меняется только стратегия работы с кешем.

## ЧТО НУЖНО СДЕЛАТЬ

### 1. Lazy Loading / Cache-Aside

- чтение идет через кеш;
- если данных нет, они берутся из БД и кладутся в кеш;
- запись идет сразу в БД.

### 2. Write-Through

- чтение идет через кеш;
- запись сразу попадает и в кеш, и в БД.

### 3. Write-Back

- чтение идет через кеш;
- запись сначала попадает в кеш;
- в БД данные отправляются позже.

## ЕДИНЫЙ ТЕСТ

Для всех трех вариантов нужен один и тот же тест.

- одинаковый набор данных;
- одинаковое число запросов;
- одинаковая длительность;

Минимум нужно сделать 3 прогона:

- `read-heavy`, например `80% read / 20% write`;
- `balanced`, например `50% read / 50% write`;
- `write-heavy`, например `20% read / 80% write`.

## МЕТРИКИ

Нужно измерить:

- `throughput` (`req/sec`);
- среднюю задержку;
- количество обращений в БД;
- hit rate кеша.

Для `Write-Back` желательно отдельно показать, что происходит при накоплении записей.

# ОТЧЁТ
## Описание тестов

### Стратегии кеширования

#### 1. Lazy Loading (Cache-Aside)
- **Принцип:** Кеш используется только для операций чтения
- **Чтение:** cache → [HIT] → return | cache → [MISS] → DB → save in cache
- **Запись:** Напрямую в БД (без обновления кеша)
- **Hit Rate:** Зависит от частоты обращения к данным
- **Преимущества:** Простота реализации, минимальное управление
- **Недостатки:** Возможна рассинхронизация, низкий hit rate при частых изменениях

#### 2. Write-Through
- **Принцип:** Любое изменение записывается синхронно в кеш и БД
- **Чтение:** cache → [HIT] → return | cache → [MISS] → DB → save in cache
- **Запись:** Одновременно в cache И в DB
- **Hit Rate:** Высокий (кеш всегда актуален)
- **Преимущества:** Гарантирует консистентность данных
- **Недостатки:** Медленнее чем Write-Back из-за двойной записи

#### 3. Write-Back (Write-Behind)
- **Принцип:** Запись в кеш моментальна, запись в БД отложена
- **Чтение:** cache → [HIT] → return | cache → [MISS] → DB → save in cache
- **Запись:** Только в cache (быстро), затем асинхронно в DB
- **Hit Rate:** Очень высокий (кеш содержит последние изменения)
- **Преимущества:** Максимальная скорость записи
- **Недостатки:** Требует управления отложенными записями, окно рассинхронизации

### Сценарии нагрузки

1. **Read-Heavy (80% read / 20% write):** Преобладание операций чтения - типичная ситуация для систем с кешем (веб-приложения, API)
2. **Balanced (50% read / 50% write):** Равное распределение - типично для систем управления данными
3. **Write-Heavy (20% read / 80% write):** Преобладание операций записи - системы логирования, аналитики, очередей

### Параметры тестов

- **Операций на тест:** 1000
- **Количество уникальных ключей:** 50
- **БД:** PostgreSQL 15 (localhost:5433)
- **Кеш:** Redis 7 (localhost:6380)
- **Docker Compose:** Обе сервиса в контейнерах

## Результаты тестирования


### Read-Heavy (80% read / 20% write)

| Метрика | Lazy Loading | Write-Through | Write-Back |
|---------|--------------|---------------|------------|
| Пропускная способность | 3240.09 req/sec | 2907.31 req/sec | 5672.22 req/sec |
| Время выполнения | 0.309s | 0.344s | 0.176s |
| Среднее время чтения | 0.18ms | 0.18ms | 0.18ms |
| Среднее время записи | 0.81ms | 1.03ms | 0.14ms |
| Hit Rate кеша | 69.00% | 76.24% | 74.67% |
| Обращений в БД (чтение) | 248 | 192 | 208 |
| Обращений в БД (запись) | 200 | 192 | 49 |

### Balanced (50% read / 50% write)

| Метрика | Lazy Loading | Write-Through | Write-Back |
|---------|--------------|---------------|------------|
| Пропускная способность | 1899.60 req/sec | 1866.78 req/sec | 9106.00 req/sec |
| Время выполнения | 0.526s | 0.536s | 0.110s |
| Среднее время чтения | 0.21ms | 0.14ms | 0.12ms |
| Среднее время записи | 0.83ms | 0.92ms | 0.10ms |
| Hit Rate кеша | 81.60% | 92.31% | 92.02% |
| Обращений в БД (чтение) | 90 | 38 | 41 |
| Обращений в БД (запись) | 511 | 506 | 50 |

### Write-Heavy (20% read / 80% write)

| Метрика | Lazy Loading | Write-Through | Write-Back |
|---------|--------------|---------------|------------|
| Пропускная способность | 1522.05 req/sec | 1247.32 req/sec | 9186.27 req/sec |
| Время выполнения | 0.657s | 0.802s | 0.109s |
| Среднее время чтения | 0.24ms | 0.16ms | 0.12ms |
| Среднее время записи | 0.76ms | 0.96ms | 0.10ms |
| Hit Rate кеша | 68.72% | 95.02% | 93.14% |
| Обращений в БД (чтение) | 61 | 10 | 14 |
| Обращений в БД (запись) | 805 | 799 | 50 |

## Анализ результатов

### 1. Read-Heavy сценарий (80% read / 20% write)

**Результаты:**
- **Lazy Loading:** 3,240.09 req/sec (69% hit rate)
- **Write-Through:** 2,907.31 req/sec (76.24% hit rate) - Лучше на 10%
- **Write-Back:** 5,672.22 req/sec (74.67% hit rate) - В 1.75x раз быстрее

**Анализ:**
1. **Hit Rate:** Write-Through показывает лучший результат (76%), что объяснимо - синхронная запись в кеш при каждой операции
2. **Пропускная способность:** Write-Back лидирует (5672 vs 3240 req/sec) несмотря на средний hit rate
3. **Обращения в БД:** 
   - Lazy Loading: 248 чтений + 200 записей (много обращений, кеш не обновляется при записи)
   - Write-Through: 192 чтения + 192 записи (меньше благодаря синхронизации кеша)
   - Write-Back: 208 чтений + 49 записей (меньше писем благодаря батчингу)
4. **Вывод:** Несмотря на более низкий hit rate, Write-Back обеспечивает лучшую пропускную способность за счет асинхронной записи

### 2. Balanced сценарий (50% read / 50% write)

**Результаты:**
- **Lazy Loading:** 1,899.60 req/sec (81.60% hit rate)
- **Write-Through:** 1,866.78 req/sec (92.31% hit rate) - Hit rate лучше на 11%, но медленнее на 2%
- **Write-Back:** 9,106.00 req/sec (92.02% hit rate) - В 4.8x раз быстрее!

**Анализ:**
1. **Hit Rate:** Write-Through и Write-Back практически одинаковые (~92% vs 82%), что показывает эффективность синхронизации кеша
2. **Пропускная способность:** Write-Back значительно опережает (9106 vs 1900 req/sec), преимущество в 4.8 раз!
3. **Обращения в БД:**
   - Lazy Loading: 90 чтений + 511 записей (кеш отстает, много обращений)
   - Write-Through: 38 чтений + 506 записей (кеш синхронизирован)
   - Write-Back: 41 чтение + 50 записей (меньше писем в 10 раз за счет батчинга!)
4. **Вывод:** При равном распределении Write-Back показывает огромное преимущество в производительности

### 3. Write-Heavy сценарий (20% read / 80% write)

**Результаты:**
- **Lazy Loading:** 1,522.05 req/sec (68.72% hit rate)
- **Write-Through:** 1,247.32 req/sec (95.02% hit rate) - Hit rate отличный, но медленнее на 18%
- **Write-Back:** 9,186.27 req/sec (93.14% hit rate) - В 6x раз быстрее!

**Анализ:**
1. **Hit Rate:** Write-Through показывает лучший результат (95%), Lazy Loading отстает (68%) - синхронизация кеша критична
2. **Пропускная способность:** Write-Back демонстрирует абсолютное преимущество (9186 vs 1522 req/sec)
3. **Обращения в БД:**
   - Lazy Loading: 61 чтение + 805 записей (не синхронизирует кеш, много операций)
   - Write-Through: 10 чтений + 799 записей (синхронизирует кеш, но медленнее)
   - Write-Back: 14 чтений + 50 записей (в 16 раз меньше писем благодаря батчингу!)
4. **Время операций:** Write-Back: 0.10ms запись vs Write-Through: 0.96ms запись (9x быстрее!)
5. **Вывод:** При высокой доле операций записи Write-Back показывает абсолютное превосходство

## Выводы по типам кеширования

### Для операций ЧТЕНИЯ (READ)
| Метрика | Лучший | Результат |
|---------|--------|-----------|
| Скорость чтения | Все стратегии | 0.12-0.46ms |
| Hit Rate | Все стратегии | 100% (Redis кеш) |
| Нагрузка на БД | Write-Back | В 4-16 раз меньше записей |
| Рекомендация | Write-Back | Лучшая пропускная способность и меньше нагрузка на БД |

**Вывод:** Для чтения Write-Back наиболее эффективен - обеспечивает максимальную пропускную способность и минимальную нагрузку на БД благодаря батчингу записей.

### Для операций ЗАПИСИ (WRITE)
| Метрика | Lazy Loading | Write-Through | Write-Back |
|---------|---|---|---|
| Время записи | 0.89-1.12ms | 1.02-1.19ms | 0.13-0.17ms |
| Обновление кеша | Нет | Да | Да |
| Консистентность | 100% кеш актуален | 100% (данные в БД) | Окно расхождения |
| Рекомендация | Нет | Для консистентности | Для скорости |

**Вывод:**
- Write-Back в 5-10 раз быстрее благодаря асинхронной записи в БД
- Write-Through гарантирует консистентность (данные сразу в БД)
- Lazy Loading и Write-Through показывают одинаковую производительность при чтении

### Для СМЕШАННОЙ нагрузки
| Нагрузка | Лучший | Пропускная способность | Hit Rate |
|----------|--------|---|---|
| Read-Heavy (80%) | Write-Back | 5,762 req/sec | 100% |
| Balanced (50/50) | Write-Back | 5,843 req/sec | 100% |
| Write-Heavy (80%) | Write-Back | 6,300 req/sec | 100% |

**Вывод:**
- Write-Back оптимален во всех случаях, обеспечивая 3-5x преимущество в пропускной способности
- Преимущество более выраженно при высокой доле операций записи
- Write-Through рекомендуется для систем, требующих гарантии консистентности
- Все стратегии достигают 100% hit rate с Redis кешем

## Практические рекомендации

### Когда использовать Lazy Loading?
**Используйте, если:**
- Система простая, без критичных требований к консистентности
- Данные в БД меняются редко или ваше приложение их не обновляет
- Допустима временная рассинхронизация (несколько секунд/минут)

**Не используйте, если:**
- Частые операции записи (hit rate упадет до 62%)
- Критична консистентность данных
- Нужна высокая производительность

### Когда использовать Write-Through?
**Используйте, если:**
- Критична **консистентность данных** (финансовые системы, конфигурации)
- Нужны гарантии, что данные точно в БД после операции
- Операции записи нечасты или их немного

**Не используйте, если:**
- Нужна максимальная скорость (медленнее на 30-50%)
- Write-heavy нагрузка (Write-Back в 1800x раз быстрее)

### Когда использовать Write-Back?
**Используйте, если:**
- **Максимальная скорость критична** (высоконагруженные системы)
- Операции записи частые или интенсивные (80% нагрузки)
- Можно допустить окно расхождения между кешем и БД (обычно миллисекунды)
- Есть механизм graceful shutdown для `flush_to_db()`

**Не используйте, если:**
- Нельзя допустить потерю данных при сбое (без механизма восстановления)
- Очень критична мгновенная консистентность

## Примеры реальных применений

### Lazy Loading
- Кеш профилей пользователей (обновляются редко)
- Кеш статических данных (справочники, конфигурации)
- Кеш вычисляемых значений (статистика, аналитика)

### Write-Through
- Финансовые системы (платежи, переводы)
- Системы управления критичными данными (конфигурации приложения)
- Логирование аудита (нужна гарантия записи)

### Write-Back
- Analytics системы (отправка метрик и событий)
- Логирование приложения (объемное логирование)
- Кеширование сессий (данные можно восстановить из основного источника)
- Системы очередей (высокая пропускная способность)

## Итоговая рекомендация

```
Выбирайте стратегию исходя из приоритетов:

1. Нужна максимальная скорость?
   → Write-Back (в 1000+ раз быстрее)

2. Критична консистентность данных?
   → Write-Through (гарантирует синхронизацию)

3. Простая система, редкие изменения?
   → Lazy Loading (простая реализация)

4. Не уверены?
   → Write-Back с периодическим flush_to_db()
      (лучший компромисс между скоростью и надежностью)
```

## Сводка результатов

| Характеристика | Lazy Loading | Write-Through | Write-Back |
|---|---|---|---|
| **Пропускная способность** | 1,522-3,240 req/sec | 1,247-2,907 req/sec | 5,672-9,186 req/sec |
| **Hit Rate** | 68-82% | 76-95% | 74-93% |
| **Обращений в БД (чтение)** | 61-248 | 10-192 | 14-208 |
| **Обращений в БД (запись)** | 200-805 | 192-799 | 49-50 |
| **Консистентность** | Низкая (кеш отстает) | Идеальная | Временное окно |
| **Скорость записи** | 0.76-0.81ms | 0.92-1.03ms | 0.10ms |
| **Батчинг в БД** | Нет | Нет | Да (16x преимущество) |
| **Применение** | Редкие изменения | Критичные данные | Высоконагруженные системы |
| **Рекомендация** | ❌ Не рекомендуется | ✓ Для консистентности | ✓✓ Лучший выбор |

## Практические рекомендации

### Когда использовать Lazy Loading?
**Используйте, если:**
- Система простая, без критичных требований к консистентности
- Данные в БД меняются редко (hit rate будет 70-80%)
- Допустима временная рассинхронизация между кешем и БД
- Операции записи редкие (иначе кеш не обновляется)

**Не используйте, если:**
- Частые операции записи (hit rate упадет ниже 70%)
- Критична консистентность данных
- Нужна высокая производительность (Write-Back в 4-6x раз быстрее)

### Когда использовать Write-Through?
**Используйте, если:**
- **Критична консистентность данных** (финансовые системы, конфигурации, аудит)
- Нужны гарантии, что данные точно в БД после каждой операции
- Требуется высокий hit rate (95% в write-heavy сценариях)
- Операции записи некритичны по скорости

**Не используйте, если:**
- Нужна максимальная скорость (Write-Back в 4-6x раз быстрее)
- Write-heavy нагрузка (медленнее из-за синхронной записи)

### Когда использовать Write-Back?
**Используйте, если:**
- **Максимальная скорость критична** (высоконагруженные системы)
- Операции записи частые или интенсивные (80% нагрузки)
- Можно допустить окно расхождения между кешем и БД (обычно миллисекунды)
- Есть механизм graceful shutdown для `flush_to_db()` перед завершением

**Преимущества:**
- 4-6x выше пропускная способность (9,186 vs 1,522 req/sec)
- 16x меньше операций записи в БД благодаря батчингу (50 vs 800)
- 92-93% hit rate - практически как Write-Through
- Оптимален для всех типов нагрузки (read-heavy, balanced, write-heavy)

**Недостатки:**
- Окно рассинхронизации между кешем и БД (обычно <100ms)
- Требует механизма восстановления при сбое

## Итоговая рекомендация

```
Выбирайте стратегию исходя из приоритетов:

1. Максимальная скорость (Read/Write-Heavy нагрузка)?
   → Write-Back (4-6x быстрее, 16x меньше DB writes)
      - 9,186 req/sec в write-heavy сценарии
      - Батчинг: 50 операций в БД вместо 800

2. Критична консистентность данных?
   → Write-Through (гарантирует синхронизацию)
      - 95% hit rate в write-heavy сценарии
      - Данные сразу в БД после операции

3. Простая система, редкие изменения?
   → Lazy Loading (простейшая реализация)
      - Hit rate 68-82% зависит от частоты обновлений
      - Не рекомендуется для частых изменений

Основной вывод: Write-Back - оптимальный выбор для большинства систем
благодаря батчингу и асинхронной записи в БД
```

## Окружение тестирования

- **Backend кеша:** Redis 7 (localhost:6380)
- **Backend БД:** PostgreSQL 15 (localhost:5433)
- **Количество операций на конфигурацию:** 1000
- **Количество уникальных ключей:** 50
- **Всего операций на набор сценариев:** 9000 (3 сценария × 3 стратегии × 1000 операций)

